<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pizzeriadonalaura">
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>

    <application
        android:name=".PizzeriaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MenuConfirmacionPedido"></activity>
        <activity android:name=".activities.MenuPedidoCarta" />
        <activity android:name=".activities.MenuPedidoPizzaPersonalizada" />
        <activity android:name=".activities.MenuPedido" />
        <activity android:name=".activities.MenuConfiguracion" />
        <activity android:name=".activities.MenuPedidoFavorita" />
        <activity android:name=".activities.MenuPrincipal" />
        <activity android:name=".activities.MainActivityLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>